this is a tumolabs WarmUp project in which should be a textarea which count the word,sentences and the letters of the given input
for run the project you can open the miniproject.html file or using liveserver extention of VsCode,
